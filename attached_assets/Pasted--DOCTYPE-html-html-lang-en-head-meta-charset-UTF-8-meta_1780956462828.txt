
  --white: #ffffff;
  --sage: #4a7c6f;
  --sage-light: #eaf3f0;
  --sage-mid: #c4ddd8;
  --amber: #c97d2e;<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>NestPortal — Tenant Dashboard</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,500;0,600;1,500&family=Plus+Jakarta+Sans:wght@300;400;500;600&display=swap" rel="stylesheet"/>
<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --cream: #f9f6f1;
  --cream2: #f2ede4;
  --amber-light: #fdf3e7;
  --amber-mid: #f0c990;
  --rose: #c0504a;
  --rose-light: #fdf0ef;
  --slate: #2c3a4a;
  --slate2: #4a5568;
  --muted: #8a8f99;
  --border: #e4ddd4;
  --border2: #ede8e0;
  --radius: 16px;
  --shadow: 0 2px 20px rgba(44,58,74,.07);
  --shadow-md: 0 6px 32px rgba(44,58,74,.11);
}

body {
  font-family: 'Plus Jakarta Sans', sans-serif;
  background: var(--cream);
  color: var(--slate);
  min-height: 100vh;
  display: flex;
}

/* ── SIDEBAR ── */
.sidebar {
  width: 230px; min-height: 100vh;
  background: var(--slate);
  display: flex; flex-direction: column;
  padding: 30px 0;
  position: fixed; left: 0; top: 0; bottom: 0;
  z-index: 50;
  animation: sideIn .5s cubic-bezier(.22,1,.36,1) both;
}
@keyframes sideIn { from { transform: translateX(-24px); opacity: 0; } }

.logo { padding: 0 22px 28px; border-bottom: 1px solid rgba(255,255,255,.08); margin-bottom: 22px; }
.logo-name { font-family: 'Cormorant Garamond', serif; font-size: 1.55rem; color: #fff; letter-spacing: .5px; }
.logo-tag { font-size: .67rem; color: rgba(255,255,255,.35); text-transform: uppercase; letter-spacing: 1.5px; margin-top: 2px; }

.nav-group { padding: 0 12px; margin-bottom: 6px; }
.nav-label { font-size: .62rem; text-transform: uppercase; letter-spacing: 1.8px; color: rgba(255,255,255,.25); padding: 6px 10px 8px; }
.nav-item {
  display: flex; align-items: center; gap: 10px;
  padding: 9px 10px; border-radius: 10px;
  font-size: .845rem; font-weight: 500; color: rgba(255,255,255,.5);
  cursor: pointer; transition: all .18s; margin-bottom: 2px;
}
.nav-item:hover { background: rgba(255,255,255,.06); color: rgba(255,255,255,.85); }
.nav-item.active { background: var(--sage); color: #fff; }
.ni { font-size: .95rem; width: 18px; text-align: center; }
.nbadge {
  margin-left: auto; background: var(--amber);
  color: #fff; font-size: .6rem; font-weight: 700;
  padding: 2px 6px; border-radius: 20px;
}

.sidebar-footer {
  margin-top: auto; padding: 20px 16px 0;
  border-top: 1px solid rgba(255,255,255,.07);
}
.s-user { display: flex; align-items: center; gap: 10px; padding: 8px; border-radius: 10px; cursor: pointer; transition: background .18s; }
.s-user:hover { background: rgba(255,255,255,.06); }
.s-av {
  width: 36px; height: 36px; border-radius: 50%;
  background: linear-gradient(135deg, var(--sage) 0%, #2a5c52 100%);
  display: grid; place-items: center;
  font-weight: 700; font-size: .8rem; color: #fff; flex-shrink: 0;
}
.s-name { font-size: .83rem; font-weight: 600; color: #fff; }
.s-role { font-size: .7rem; color: rgba(255,255,255,.35); }

/* ── MAIN ── */
.main { margin-left: 230px; flex: 1; padding: 32px 36px 48px; }

/* ── PAGE HEADER ── */
.topbar { display: flex; align-items: flex-start; justify-content: space-between; margin-bottom: 30px; animation: up .5s .05s both; }
@keyframes up { from { opacity: 0; transform: translateY(14px); } }
.page-title { font-family: 'Cormorant Garamond', serif; font-size: 1.9rem; font-weight: 600; color: var(--slate); line-height: 1.1; }
.page-sub { font-size: .82rem; color: var(--muted); margin-top: 4px; }
.top-actions { display: flex; align-items: center; gap: 10px; }
.icon-btn {
  width: 38px; height: 38px; border-radius: 10px;
  background: var(--white); border: 1.5px solid var(--border);
  display: grid; place-items: center; font-size: .95rem;
  cursor: pointer; position: relative; transition: border-color .18s, box-shadow .18s;
}
.icon-btn:hover { border-color: var(--sage); box-shadow: var(--shadow); }
.dot-badge {
  position: absolute; top: 6px; right: 6px;
  width: 7px; height: 7px; border-radius: 50%;
  background: var(--rose); border: 2px solid var(--white);
}
.pay-btn {
  background: var(--sage); color: #fff; border: none;
  border-radius: 10px; padding: 9px 20px;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .875rem; font-weight: 600; cursor: pointer;
  display: flex; align-items: center; gap: 7px;
  transition: all .18s;
  box-shadow: 0 4px 14px rgba(74,124,111,.3);
}
.pay-btn:hover { transform: translateY(-1px); box-shadow: 0 6px 22px rgba(74,124,111,.4); }

/* ── HERO RENT CARD ── */
.hero-row { display: grid; grid-template-columns: 1.2fr 1fr 1fr; gap: 18px; margin-bottom: 22px; animation: up .5s .1s both; }

.hero-card {
  background: var(--slate);
  border-radius: var(--radius);
  padding: 28px 26px;
  position: relative; overflow: hidden;
  color: #fff;
}
.hero-card::before {
  content: '';
  position: absolute; right: -30px; top: -30px;
  width: 160px; height: 160px; border-radius: 50%;
  background: radial-gradient(circle, rgba(74,124,111,.35) 0%, transparent 70%);
}
.hero-card::after {
  content: '';
  position: absolute; right: 30px; bottom: -40px;
  width: 100px; height: 100px; border-radius: 50%;
  background: radial-gradient(circle, rgba(201,125,46,.2) 0%, transparent 70%);
}
.hero-label { font-size: .7rem; text-transform: uppercase; letter-spacing: 1.2px; color: rgba(255,255,255,.45); margin-bottom: 8px; }
.hero-amount { font-family: 'Cormorant Garamond', serif; font-size: 2.6rem; font-weight: 600; line-height: 1; margin-bottom: 6px; }
.hero-unit { font-size: .8rem; color: rgba(255,255,255,.55); margin-bottom: 20px; }
.due-row { display: flex; align-items: center; justify-content: space-between; }
.due-chip {
  background: rgba(201,125,46,.25); color: var(--amber-mid);
  font-size: .72rem; font-weight: 600; padding: 4px 12px; border-radius: 20px;
}
.due-date { font-size: .75rem; color: rgba(255,255,255,.4); }

.mini-stat {
  background: var(--white); border-radius: var(--radius);
  border: 1.5px solid var(--border2);
  padding: 22px; display: flex; flex-direction: column;
  justify-content: space-between;
  box-shadow: var(--shadow);
  transition: box-shadow .18s, transform .18s;
}
.mini-stat:hover { box-shadow: var(--shadow-md); transform: translateY(-2px); }
.ms-top { display: flex; align-items: center; justify-content: space-between; }
.ms-icon {
  width: 38px; height: 38px; border-radius: 10px;
  display: grid; place-items: center; font-size: 1rem;
}
.ms-label { font-size: .7rem; text-transform: uppercase; letter-spacing: 1px; color: var(--muted); margin: 12px 0 4px; }
.ms-value { font-family: 'Cormorant Garamond', serif; font-size: 1.75rem; color: var(--slate); line-height: 1; }
.ms-sub { font-size: .72rem; color: var(--muted); margin-top: 4px; }
.trend { font-size: .72rem; font-weight: 600; }
.trend.good { color: var(--sage); }
.trend.warn { color: var(--amber); }

/* ── MID ROW ── */
.mid-row { display: grid; grid-template-columns: 1fr 320px; gap: 18px; margin-bottom: 22px; animation: up .5s .2s both; }

/* ── PAYMENT HISTORY ── */
.card {
  background: var(--white); border-radius: var(--radius);
  border: 1.5px solid var(--border2);
  padding: 24px; box-shadow: var(--shadow);
}
.card-hd { display: flex; align-items: center; justify-content: space-between; margin-bottom: 20px; }
.card-title { font-family: 'Cormorant Garamond', serif; font-size: 1.15rem; color: var(--slate); }
.card-sub { font-size: .74rem; color: var(--muted); margin-top: 2px; }
.view-all { font-size: .78rem; color: var(--sage); text-decoration: none; font-weight: 500; }
.view-all:hover { text-decoration: underline; }

/* mini sparkline */
.spark { display: flex; align-items: flex-end; gap: 5px; height: 52px; margin-bottom: 20px; }
.spark-bar {
  flex: 1; border-radius: 4px 4px 0 0;
  transition: opacity .18s; cursor: pointer;
}
.spark-bar:hover { opacity: .75; }
.spark-labels { display: flex; gap: 5px; }
.spark-lbl { flex: 1; text-align: center; font-size: .6rem; color: var(--muted); }

.pay-row {
  display: flex; align-items: center; gap: 12px;
  padding: 11px 10px; border-radius: 10px;
  transition: background .15s; cursor: pointer;
}
.pay-row:hover { background: var(--cream); }
.pay-month {
  width: 38px; height: 38px; border-radius: 10px;
  display: grid; place-items: center; flex-shrink: 0;
  font-size: .7rem; font-weight: 700;
}
.pay-desc { flex: 1; }
.pay-name { font-size: .875rem; font-weight: 500; color: var(--slate); }
.pay-date { font-size: .72rem; color: var(--muted); margin-top: 1px; }
.pay-amount { font-size: .9rem; font-weight: 600; color: var(--slate); }
.pay-status {
  font-size: .67rem; font-weight: 700; text-transform: uppercase;
  padding: 3px 8px; border-radius: 20px; letter-spacing: .4px;
}
.ps-paid { background: var(--sage-light); color: var(--sage); }
.ps-pending { background: var(--amber-light); color: var(--amber); }
.ps-late { background: var(--rose-light); color: var(--rose); }

/* ── LEASE CARD ── */
.lease-card { display: flex; flex-direction: column; }
.lease-banner {
  background: linear-gradient(135deg, var(--sage) 0%, #2a5c52 100%);
  border-radius: 12px; padding: 20px;
  color: #fff; margin-bottom: 16px; position: relative; overflow: hidden;
}
.lease-banner::after {
  content: '🏠'; position: absolute; right: 16px; bottom: -4px;
  font-size: 3.5rem; opacity: .15;
}
.lb-label { font-size: .65rem; text-transform: uppercase; letter-spacing: 1.2px; opacity: .6; margin-bottom: 4px; }
.lb-addr { font-size: .95rem; font-weight: 600; line-height: 1.35; margin-bottom: 14px; }
.lease-progress-wrap { }
.lp-row { display: flex; justify-content: space-between; font-size: .7rem; opacity: .7; margin-bottom: 5px; }
.lp-bar { height: 5px; background: rgba(255,255,255,.2); border-radius: 3px; overflow: hidden; }
.lp-fill { height: 100%; background: var(--amber-mid); border-radius: 3px; width: 62%; transition: width 1s .5s cubic-bezier(.22,1,.36,1); }

.lease-details { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 16px; }
.ld-item { background: var(--cream); border-radius: 10px; padding: 12px; }
.ld-label { font-size: .67rem; text-transform: uppercase; letter-spacing: .8px; color: var(--muted); margin-bottom: 3px; }
.ld-val { font-size: .875rem; font-weight: 600; color: var(--slate); }

.docs-list { }
.doc-row {
  display: flex; align-items: center; gap: 10px;
  padding: 9px 10px; border-radius: 9px;
  transition: background .15s; cursor: pointer;
}
.doc-row:hover { background: var(--cream); }
.doc-icon { width: 32px; height: 32px; border-radius: 8px; background: var(--cream2); display: grid; place-items: center; font-size: .85rem; flex-shrink: 0; }
.doc-name { font-size: .82rem; font-weight: 500; color: var(--slate); flex: 1; }
.doc-dl { font-size: .7rem; color: var(--sage); font-weight: 600; }

/* ── BOTTOM ROW ── */
.bottom-row { display: grid; grid-template-columns: 1fr 1fr; gap: 18px; animation: up .5s .3s both; }

/* ── MAINTENANCE ── */
.new-req-btn {
  background: var(--sage-light); color: var(--sage);
  border: 1.5px solid var(--sage-mid);
  border-radius: 8px; padding: 6px 14px;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: .78rem; font-weight: 600; cursor: pointer;
  transition: background .15s;
}
.new-req-btn:hover { background: var(--sage-mid); }

.maint-item {
  display: flex; gap: 12px; padding: 11px 10px;
  border-radius: 10px; transition: background .15s; cursor: pointer;
}
.maint-item:hover { background: var(--cream); }
.maint-icon { width: 36px; height: 36px; border-radius: 9px; display: grid; place-items: center; font-size: .9rem; flex-shrink: 0; }
.maint-title { font-size: .875rem; font-weight: 500; color: var(--slate); }
.maint-meta { font-size: .72rem; color: var(--muted); margin-top: 2px; }
.maint-status {
  margin-left: auto; font-size: .67rem; font-weight: 700;
  padding: 3px 8px; border-radius: 20px; align-self: flex-start; white-space: nowrap;
}
.ms-open { background: var(--amber-light); color: var(--amber); }
.ms-prog { background: var(--sage-light); color: var(--sage); }
.ms-done { background: #eef2ff; color: #5b6cf0; }

/* ── NOTICES ── */
.notice-item {
  display: flex; gap: 12px; padding: 13px;
  border-radius: 10px; border: 1.5px solid var(--border2);
  margin-bottom: 10px; transition: box-shadow .15s; cursor: pointer;
}
.notice-item:last-child { margin-bottom: 0; }
.notice-item:hover { box-shadow: var(--shadow); }
.notice-dot { width: 8px; height: 8px; border-radius: 50%; margin-top: 5px; flex-shrink: 0; }
.notice-title { font-size: .875rem; font-weight: 500; color: var(--slate); margin-bottom: 3px; }
.notice-body { font-size: .78rem; color: var(--muted); line-height: 1.5; }
.notice-time { font-size: .68rem; color: var(--muted); margin-top: 5px; }

/* ── RESPONSIVE ── */
@media (max-width: 1080px) {
  .hero-row { grid-template-columns: 1fr 1fr; }
  .hero-card { grid-column: 1 / -1; }
  .mid-row, .bottom-row { grid-template-columns: 1fr; }
}
@media (max-width: 760px) {
  .sidebar { width: 56px; }
  .logo-name, .logo-tag, .nav-item span:not(.ni), .nav-label, .s-name, .s-role { display: none; }
  .main { margin-left: 56px; padding: 18px; }
  .hero-row { grid-template-columns: 1fr; }
}
</style>
</head>
<body>

<!-- SIDEBAR -->
<aside class="sidebar">
  <div class="logo">
    <div class="logo-name">NestPortal</div>
    <div class="logo-tag">Tenant Portal</div>
  </div>

  <div class="nav-group">
    <div class="nav-label">Overview</div>
    <div class="nav-item active"><span class="ni">🏠</span><span>Dashboard</span></div>
    <div class="nav-item"><span class="ni">💳</span><span>Rent & Payments</span></div>
    <div class="nav-item"><span class="ni">📄</span><span>My Lease</span></div>
  </div>

  <div class="nav-group">
    <div class="nav-label">Support</div>
    <div class="nav-item"><span class="ni">🔧</span><span>Maintenance</span><span class="nbadge">2</span></div>
    <div class="nav-item"><span class="ni">📢</span><span>Notices</span><span class="nbadge">1</span></div>
    <div class="nav-item"><span class="ni">💬</span><span>Messages</span></div>
    <div class="nav-item"><span class="ni">📎</span><span>Documents</span></div>
  </div>

  <div class="nav-group">
    <div class="nav-label">Account</div>
    <div class="nav-item"><span class="ni">⚙️</span><span>Settings</span></div>
  </div>

  <div class="sidebar-footer">
    <div class="s-user">
      <div class="s-av">MW</div>
      <div>
        <div class="s-name">Mercy Wanjiku</div>
        <div class="s-role">Unit A-104</div>
      </div>
    </div>
  </div>
</aside>

<!-- MAIN -->
<main class="main">

  <!-- TOPBAR -->
  <div class="topbar">
    <div>
      <div class="page-title">Welcome back, Mercy</div>
      <div class="page-sub">Tuesday, June 9, 2026 · Unit A-104 · Sunrise Apartments</div>
    </div>
    <div class="top-actions">
      <div class="icon-btn">🔔<span class="dot-badge"></span></div>
      <div class="icon-btn">📩</div>
      <button class="pay-btn">💳 Pay Rent</button>
    </div>
  </div>

  <!-- HERO ROW -->
  <div class="hero-row">

    <!-- Rent Due Card -->
    <div class="hero-card">
      <div class="hero-label">Rent Due</div>
      <div class="hero-amount">Ksh 18,000</div>
      <div class="hero-unit">1-Bedroom · Sunrise Apts, Block A</div>
      <div class="due-row">
        <span class="due-chip">⏰ Due in 6 days</span>
        <span class="due-date">15 Jun 2026</span>
      </div>
    </div>

    <!-- Lease Remaining -->
    <div class="mini-stat">
      <div class="ms-top">
        <div class="ms-icon" style="background:var(--sage-light)">📅</div>
        <span class="trend good">62% elapsed</span>
      </div>
      <div>
        <div class="ms-label">Lease Remaining</div>
        <div class="ms-value">5 mo.</div>
        <div class="ms-sub">Expires Nov 30, 2026</div>
      </div>
    </div>

    <!-- Total Paid -->
    <div class="mini-stat">
      <div class="ms-top">
        <div class="ms-icon" style="background:var(--amber-light)">✅</div>
        <span class="trend good">On time</span>
      </div>
      <div>
        <div class="ms-label">Paid This Year</div>
        <div class="ms-value">Ksh 126k</div>
        <div class="ms-sub">7 payments · 0 late</div>
      </div>
    </div>

  </div>

  <!-- MID ROW -->
  <div class="mid-row">

    <!-- Payment History -->
    <div class="card">
      <div class="card-hd">
        <div>
          <div class="card-title">Payment History</div>
          <div class="card-sub">Last 6 months — 2026</div>
        </div>
        <a href="#" class="view-all">View all →</a>
      </div>

      <!-- Sparkline -->
      <div class="spark" id="sparkline"></div>
      <div class="spark-labels" id="sparkLabels"></div>

      <div style="margin-top:16px" id="payList"></div>
    </div>

    <!-- Lease & Docs -->
    <div class="card lease-card">
      <div class="card-hd">
        <div>
          <div class="card-title">My Lease</div>
          <div class="card-sub">Active tenancy</div>
        </div>
      </div>

      <div class="lease-banner">
        <div class="lb-label">Property</div>
        <div class="lb-addr">Unit A-104, Sunrise Apartments<br/>Kiambu Road, Nairobi</div>
        <div class="lease-progress-wrap">
          <div class="lp-row"><span>Start: Dec 1, 2025</span><span>End: Nov 30, 2026</span></div>
          <div class="lp-bar"><div class="lp-fill" id="lpFill"></div></div>
        </div>
      </div>

      <div class="lease-details">
        <div class="ld-item">
          <div class="ld-label">Monthly Rent</div>
          <div class="ld-val">Ksh 18,000</div>
        </div>
        <div class="ld-item">
          <div class="ld-label">Deposit Held</div>
          <div class="ld-val">Ksh 36,000</div>
        </div>
        <div class="ld-item">
          <div class="ld-label">Lease Type</div>
          <div class="ld-val">12-Month</div>
        </div>
        <div class="ld-item">
          <div class="ld-label">Landlord</div>
          <div class="ld-val">James Kamau</div>
        </div>
      </div>

      <div class="card-hd" style="margin-bottom:10px;padding-top:4px;border-top:1.5px solid var(--border2)">
        <div class="card-title" style="font-size:.95rem">Documents</div>
        <a href="#" class="view-all">All docs →</a>
      </div>
      <div class="docs-list">
        <div class="doc-row">
          <div class="doc-icon">📋</div>
          <div class="doc-name">Tenancy Agreement 2025–26</div>
          <span class="doc-dl">↓ PDF</span>
        </div>
        <div class="doc-row">
          <div class="doc-icon">🧾</div>
          <div class="doc-name">Deposit Receipt</div>
          <span class="doc-dl">↓ PDF</span>
        </div>
        <div class="doc-row">
          <div class="doc-icon">🏠</div>
          <div class="doc-name">Move-in Inspection Report</div>
          <span class="doc-dl">↓ PDF</span>
        </div>
      </div>
    </div>

  </div>

  <!-- BOTTOM ROW -->
  <div class="bottom-row">

    <!-- Maintenance -->
    <div class="card">
      <div class="card-hd">
        <div>
          <div class="card-title">Maintenance Requests</div>
          <div class="card-sub">2 active tickets</div>
        </div>
        <button class="new-req-btn">+ New Request</button>
      </div>
      <div id="maintList"></div>
    </div>

    <!-- Notices -->
    <div class="card">
      <div class="card-hd">
        <div>
          <div class="card-title">Notices & Announcements</div>
          <div class="card-sub">From your landlord</div>
        </div>
        <a href="#" class="view-all">All →</a>
      </div>
      <div id="noticeList"></div>
    </div>

  </div>

</main>

<script>
// ── SPARKLINE ──
const months6 = ['Jan','Feb','Mar','Apr','May','Jun'];
const paid6   = [18000,18000,18000,18000,18000,0];
const maxP = 20000;
const