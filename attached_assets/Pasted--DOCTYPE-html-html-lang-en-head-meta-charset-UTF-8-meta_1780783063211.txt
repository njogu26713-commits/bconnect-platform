<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>NIVEA Rich Nourishing — BConnect</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet"/>
<style>
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --navy: #003b6e;
    --navy-deep: #001f3f;
    --sky: #0077cc;
    --sky-light: #e8f4ff;
    --accent: #0094ff;
    --gold: #f0a500;
    --surface: #f7f9fc;
    --white: #ffffff;
    --text: #1a2535;
    --muted: #6b7a91;
    --border: #dde4ef;
    --radius: 14px;
    --shadow: 0 4px 24px rgba(0,60,120,.10);
  }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--surface);
    color: var(--text);
    min-height: 100vh;
  }

  /* ── NAV ── */
  nav {
    background: var(--navy-deep);
    padding: 0 32px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    position: sticky; top: 0; z-index: 100;
    box-shadow: 0 2px 16px rgba(0,0,0,.3);
  }
  .nav-logo {
    display: flex; align-items: center; gap: 10px;
    font-family: 'DM Serif Display', serif;
    color: var(--white); font-size: 1.35rem; letter-spacing: .5px;
  }
  .nav-logo span {
    width: 32px; height: 32px; border-radius: 50%;
    background: var(--accent);
    display: grid; place-items: center;
    font-size: .75rem; font-weight: 700; color: #fff;
    font-family: 'DM Sans', sans-serif;
  }
  .nav-links { display: flex; gap: 24px; }
  .nav-links a {
    color: rgba(255,255,255,.7); text-decoration: none;
    font-size: .875rem; transition: color .2s;
  }
  .nav-links a:hover { color: #fff; }
  .nav-cart {
    position: relative; cursor: pointer;
    color: #fff; font-size: 1.1rem;
  }
  .nav-cart .badge {
    position: absolute; top: -6px; right: -8px;
    background: var(--gold); color: #fff;
    width: 16px; height: 16px; border-radius: 50%;
    font-size: .6rem; display: grid; place-items: center; font-weight: 700;
  }

  /* ── BREADCRUMB ── */
  .breadcrumb {
    padding: 14px 48px;
    font-size: .8rem; color: var(--muted);
    display: flex; gap: 6px; align-items: center;
  }
  .breadcrumb a { color: var(--sky); text-decoration: none; }
  .breadcrumb a:hover { text-decoration: underline; }
  .breadcrumb .sep { opacity: .4; }

  /* ── MAIN LAYOUT ── */
  .page {
    max-width: 1180px;
    margin: 0 auto;
    padding: 0 24px 64px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 48px;
    align-items: start;
  }

  /* ── GALLERY ── */
  .gallery { position: sticky; top: 72px; }
  .main-img-wrap {
    background: #fff;
    border-radius: var(--radius);
    border: 1.5px solid var(--border);
    overflow: hidden;
    aspect-ratio: 1;
    display: flex; align-items: center; justify-content: center;
    position: relative;
    box-shadow: var(--shadow);
  }
  .main-img-wrap img {
    width: 65%; height: 65%; object-fit: contain;
    transition: transform .4s cubic-bezier(.22,1,.36,1);
  }
  .main-img-wrap:hover img { transform: scale(1.06); }
  .badge-new {
    position: absolute; top: 16px; left: 16px;
    background: var(--gold); color: #fff;
    font-size: .7rem; font-weight: 700;
    padding: 4px 10px; border-radius: 20px;
    letter-spacing: .5px; text-transform: uppercase;
  }
  .thumbs {
    display: flex; gap: 10px; margin-top: 14px;
  }
  .thumb {
    flex: 1; background: #fff; border-radius: 10px;
    border: 1.5px solid var(--border);
    aspect-ratio: 1; display: flex;
    align-items: center; justify-content: center;
    cursor: pointer; overflow: hidden;
    transition: border-color .2s, box-shadow .2s;
  }
  .thumb.active, .thumb:hover {
    border-color: var(--accent);
    box-shadow: 0 0 0 3px rgba(0,148,255,.15);
  }
  .thumb img { width: 60%; height: 60%; object-fit: contain; }

  /* ── PRODUCT INFO ── */
  .info { padding-top: 4px; }
  .seller-row {
    display: flex; align-items: center; gap: 8px; margin-bottom: 10px;
  }
  .seller-avatar {
    width: 28px; height: 28px; border-radius: 50%;
    background: var(--navy); color: #fff;
    display: grid; place-items: center;
    font-size: .75rem; font-weight: 700;
  }
  .seller-name { font-size: .85rem; font-weight: 500; color: var(--muted); }
  .seller-location { font-size: .75rem; color: var(--muted); display: flex; align-items: center; gap: 3px; }
  .verified-badge {
    margin-left: auto;
    font-size: .72rem; color: #22a84a; font-weight: 600;
    display: flex; align-items: center; gap: 3px;
  }

  .product-title {
    font-family: 'DM Serif Display', serif;
    font-size: 2rem; line-height: 1.2;
    color: var(--navy-deep);
    margin-bottom: 8px;
  }
  .product-subtitle {
    font-size: .9rem; color: var(--muted);
    margin-bottom: 18px; line-height: 1.55;
  }

  .stars {
    display: flex; align-items: center; gap: 6px; margin-bottom: 20px;
  }
  .stars .s { color: var(--gold); font-size: 1rem; letter-spacing: 1px; }
  .stars .count { font-size: .8rem; color: var(--muted); }

  /* ── VARIANTS ── */
  .section-label {
    font-size: .75rem; font-weight: 600; text-transform: uppercase;
    letter-spacing: 1px; color: var(--muted); margin-bottom: 10px;
  }
  .variants {
    display: grid; grid-template-columns: repeat(4, 1fr);
    gap: 10px; margin-bottom: 24px;
  }
  .variant {
    border: 1.5px solid var(--border); border-radius: 10px;
    padding: 10px 6px; text-align: center;
    cursor: pointer; background: #fff;
    transition: all .2s; position: relative;
  }
  .variant:hover { border-color: var(--accent); }
  .variant.active {
    border-color: var(--accent);
    background: var(--sky-light);
    box-shadow: 0 0 0 3px rgba(0,148,255,.12);
  }
  .variant.active::after {
    content: '✓';
    position: absolute; top: 5px; right: 7px;
    font-size: .65rem; color: var(--accent); font-weight: 700;
  }
  .variant img { width: 40px; height: 48px; object-fit: contain; display: block; margin: 0 auto 6px; }
  .variant .v-size { font-size: .78rem; font-weight: 600; color: var(--text); }
  .variant .v-price { font-size: .72rem; color: var(--sky); font-weight: 600; }

  /* ── QUANTITY ── */
  .qty-row {
    display: flex; align-items: center; gap: 16px; margin-bottom: 28px;
  }
  .qty-ctrl {
    display: flex; align-items: center; gap: 0;
    border: 1.5px solid var(--border); border-radius: 10px;
    overflow: hidden; background: #fff;
  }
  .qty-btn {
    width: 38px; height: 38px; border: none; background: transparent;
    font-size: 1.1rem; cursor: pointer; color: var(--navy);
    transition: background .15s;
    display: grid; place-items: center;
  }
  .qty-btn:hover { background: var(--sky-light); }
  .qty-val {
    width: 42px; text-align: center;
    font-weight: 600; font-size: .95rem; color: var(--text);
    border-left: 1.5px solid var(--border);
    border-right: 1.5px solid var(--border);
    height: 38px; display: grid; place-items: center;
  }
  .stock-info { font-size: .8rem; color: #22a84a; font-weight: 500; }

  /* ── PRICE ── */
  .price-row {
    display: flex; align-items: baseline; gap: 10px; margin-bottom: 24px;
  }
  .price-main {
    font-family: 'DM Serif Display', serif;
    font-size: 2.1rem; color: var(--navy-deep);
  }
  .price-currency { font-size: 1rem; font-family: 'DM Sans', sans-serif; font-weight: 600; }
  .price-old {
    font-size: .95rem; color: var(--muted);
    text-decoration: line-through;
  }
  .price-save {
    font-size: .75rem; background: #fff0cc;
    color: #b07800; padding: 2px 8px; border-radius: 20px; font-weight: 600;
  }

  /* ── ACTIONS ── */
  .actions { display: flex; flex-direction: column; gap: 10px; margin-bottom: 24px; }
  .btn {
    padding: 14px 20px; border-radius: 10px;
    font-family: 'DM Sans', sans-serif;
    font-size: .95rem; font-weight: 600;
    cursor: pointer; border: none;
    transition: all .2s; display: flex;
    align-items: center; justify-content: center; gap: 8px;
  }
  .btn-check {
    background: #fff; border: 1.5px solid var(--border);
    color: var(--navy);
  }
  .btn-check:hover { border-color: var(--accent); color: var(--accent); }
  .btn-cart {
    background: #fff; border: 1.5px solid var(--accent);
    color: var(--accent);
  }
  .btn-cart:hover { background: var(--sky-light); }
  .btn-buy {
    background: linear-gradient(135deg, var(--sky) 0%, var(--navy) 100%);
    color: #fff;
    box-shadow: 0 4px 14px rgba(0,119,204,.35);
  }
  .btn-buy:hover { transform: translateY(-1px); box-shadow: 0 6px 20px rgba(0,119,204,.45); }

  /* ── ABOUT ── */
  .about {
    background: #fff; border-radius: var(--radius);
    border: 1.5px solid var(--border);
    padding: 20px;
    margin-bottom: 24px;
  }
  .about h3 {
    font-size: .75rem; font-weight: 700;
    text-transform: uppercase; letter-spacing: 1px;
    color: var(--muted); margin-bottom: 10px;
  }
  .about p { font-size: .875rem; color: #3a4a5e; line-height: 1.65; }
  .tags { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 12px; }
  .tag {
    font-size: .72rem; background: var(--sky-light);
    color: var(--sky); border-radius: 20px;
    padding: 3px 10px; font-weight: 500;
  }

  /* ── MORE IN PRODUCT ── */
  .more-section {
    grid-column: 1 / -1;
    padding-top: 8px;
  }
  .more-header {
    display: flex; align-items: center; justify-content: space-between;
    margin-bottom: 18px;
  }
  .more-header h2 {
    font-family: 'DM Serif Display', serif;
    font-size: 1.4rem; color: var(--navy-deep);
  }
  .more-header a { font-size: .85rem; color: var(--sky); text-decoration: none; }
  .more-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 16px;
  }
  .more-card {
    background: #fff; border-radius: var(--radius);
    border: 1.5px solid var(--border);
    overflow: hidden;
    transition: box-shadow .2s, transform .2s;
    cursor: pointer;
  }
  .more-card:hover {
    box-shadow: var(--shadow); transform: translateY(-3px);
  }
  .more-card .mc-img {
    background: var(--sky-light);
    aspect-ratio: 1; display: flex;
    align-items: center; justify-content: center;
  }
  .more-card .mc-img img { width: 70%; height: 70%; object-fit: contain; }
  .more-card .mc-info { padding: 12px 14px; }
  .more-card .mc-name { font-size: .85rem; font-weight: 600; color: var(--text); margin-bottom: 2px; }
  .more-card .mc-brand { font-size: .75rem; color: var(--muted); margin-bottom: 6px; }
  .more-card .mc-price { font-size: .9rem; font-weight: 700; color: var(--sky); }

  /* placeholder product images using CSS */
  .prod-icon {
    width: 70%; height: 70%;
    display: flex; flex-direction: column;
    align-items: center; justify-content: center;
  }

  @media (max-width: 768px) {
    .page { grid-template-columns: 1fr; gap: 24px; padding: 0 16px 48px; }
    .variants { grid-template-columns: repeat(2, 1fr); }
    .more-grid { grid-template-columns: repeat(2, 1fr); }
    .gallery { position: static; }
    .breadcrumb { padding: 12px 16px; }
  }
</style>
</head>
<body>

<!-- NAV -->
<nav>
  <div class="nav-logo">
    <span>B</span> BConnect
  </div>
  <div class="nav-links">
    <a href="#">Home</a>
    <a href="#">Shop</a>
    <a href="#">Categories</a>
    <a href="#">Deals</a>
  </div>
  <div class="nav-cart">
    🛒
    <span class="badge">3</span>
  </div>
</nav>

<!-- BREADCRUMB -->
<div class="breadcrumb">
  <a href="#">Home</a><span class="sep">›</span>
  <a href="#">Skincare</a><span class="sep">›</span>
  <a href="#">Body Lotion</a><span class="sep">›</span>
  <span>NIVEA Rich Nourishing</span>
</div>

<!-- MAIN -->
<div class="page">

  <!-- GALLERY -->
  <div class="gallery">
    <div class="main-img-wrap" id="mainImg">
      <span class="badge-new">Bestseller</span>
      <!-- NIVEA bottle SVG placeholder -->
      <svg viewBox="0 0 120 200" width="200" height="260" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <linearGradient id="bottleGrad" x1="0" y1="0" x2="1" y2="1">
            <stop offset="0%" stop-color="#003b6e"/>
            <stop offset="100%" stop-color="#0077cc"/>
          </linearGradient>
        </defs>
        <!-- bottle body -->
        <rect x="25" y="55" width="70" height="120" rx="16" fill="url(#bottleGrad)"/>
        <!-- cap -->
        <rect x="38" y="30" width="44" height="30" rx="8" fill="#001f3f"/>
        <!-- label highlight -->
        <rect x="30" y="80" width="60" height="70" rx="6" fill="rgba(255,255,255,.08)"/>
        <!-- NIVEA text -->
        <text x="60" y="107" text-anchor="middle" fill="#fff" font-size="11" font-weight="700" font-family="sans-serif">NIVEA</text>
        <text x="60" y="122" text-anchor="middle" fill="rgba(255,255,255,.75)" font-size="6.5" font-family="sans-serif">body lotion</text>
        <text x="60" y="133" text-anchor="middle" fill="#fff" font-size="8.5" font-weight="600" font-family="sans-serif">Rich Nourishing</text>
        <text x="60" y="144" text-anchor="middle" fill="rgba(255,255,255,.6)" font-size="6" font-family="sans-serif">48h moisture care</text>
        <!-- drop icon -->
        <ellipse cx="60" cy="162" rx="8" ry="10" fill="rgba(255,255,255,.18)"/>
        <polygon points="60,152 54,165 66,165" fill="rgba(255,255,255,.18)"/>
      </svg>
    </div>
    <div class="thumbs">
      <div class="thumb active" onclick="selectThumb(this, '#003b6e')">
        <svg viewBox="0 0 60 90" width="40" height="56" xmlns="http://www.w3.org/2000/svg">
          <rect x="10" y="18" width="40" height="62" rx="8" fill="#003b6e"/>
          <rect x="18" y="8" width="24" height="14" rx="4" fill="#001f3f"/>
          <text x="30" y="48" text-anchor="middle" fill="#fff" font-size="6" font-weight="700" font-family="sans-serif">NIVEA</text>
        </svg>
      </div>
      <div class="thumb" onclick="selectThumb(this, '#1a6fa8')">
        <svg viewBox="0 0 60 90" width="40" height="56" xmlns="http://www.w3.org/2000/svg">
          <rect x="8" y="15" width="44" height="68" rx="9" fill="#1a6fa8"/>
          <rect x="18" y="5" width="24" height="14" rx="4" fill="#0d3d63"/>
          <text x="30" y="50" text-anchor="middle" fill="#fff" font-size="6" font-weight="700" font-family="sans-serif">NIVEA</text>
        </svg>
      </div>
      <div class="thumb" onclick="selectThumb(this, '#005fa3')">
        <svg viewBox="0 0 60 90" width="40" height="56" xmlns="http://www.w3.org/2000/svg">
          <rect x="5" y="10" width="50" height="75" rx="10" fill="#005fa3"/>
          <rect x="16" y="2" width="28" height="12" rx="4" fill="#003b6e"/>
          <text x="30" y="52" text-anchor="middle" fill="#fff" font-size="6" font-weight="700" font-family="sans-serif">NIVEA</text>
        </svg>
      </div>
      <div class="thumb" onclick="selectThumb(this, '#e8f4ff')">
        <svg viewBox="0 0 60 90" width="40" height="56" xmlns="http://www.w3.org/2000/svg">
          <rect x="8" y="15" width="44" height="68" rx="9" fill="#e8f4ff" stroke="#b0cce4" stroke-width="1.5"/>
          <rect x="18" y="5" width="24" height="14" rx="4" fill="#cce0f0"/>
          <text x="30" y="50" text-anchor="middle" fill="#003b6e" font-size="6" font-weight="700" font-family="sans-serif">NIVEA</text>
        </svg>
      </div>
    </div>
  </div>

  <!-- PRODUCT INFO -->
  <div class="info">

    <!-- Seller -->
    <div class="seller-row">
      <div class="seller-avatar">B</div>
      <div>
        <div class="seller-name">Brian Githinji</div>
        <div class="seller-location">📍 Kutus</div>
      </div>
      <div class="verified-badge">✔ Verified</div>
    </div>

    <h1 class="product-title">NIVEA Rich Nourishing<br/>Body Lotion</h1>
    <p class="product-subtitle">48-hour intensive moisture care with Deep Moisture Serum &amp; natural almond oil. Smooth, non-greasy formula.</p>

    <div class="stars">
      <span class="s">★★★★★</span>
      <span class="count">4.8 (312 reviews)</span>
    </div>

    <!-- Variants -->
    <div class="section-label">Pick a size</div>
    <div class="variants" id="variants">
      <div class="variant active" data-price="300" onclick="selectVariant(this)">
        <svg viewBox="0 0 40 60" width="36" height="48" xmlns="http://www.w3.org/2000/svg">
          <rect x="6" y="12" width="28" height="44" rx="6" fill="#003b6e"/>
          <rect x="12" y="5" width="16" height="10" rx="3" fill="#001f3f"/>
        </svg>
        <div class="v-size">100 ml</div>
        <div class="v-price">Ksh 300</div>
      </div>
      <div class="variant" data-price="650" onclick="selectVariant(this)">
        <svg viewBox="0 0 40 60" width="36" height="48" xmlns="http://www.w3.org/2000/svg">
          <rect x="4" y="10" width="32" height="48" rx="7" fill="#003b6e"/>
          <rect x="12" y="3" width="16" height="10" rx="3" fill="#001f3f"/>
        </svg>
        <div class="v-size">200 ml</div>
        <div class="v-price">Ksh 650</div>
      </div>
      <div class="variant" data-price="2600" onclick="selectVariant(this)">
        <svg viewBox="0 0 40 70" width="36" height="52" xmlns="http://www.w3.org/2000/svg">
          <rect x="2" y="8" width="36" height="58" rx="8" fill="#003b6e"/>
          <rect x="11" y="2" width="18" height="10" rx="3" fill="#001f3f"/>
        </svg>
        <div class="v-size">400 ml</div>
        <div class="v-price">Ksh 2,600</div>
      </div>
      <div class="variant" data-price="3500" onclick="selectVariant(this)">
        <svg viewBox="0 0 44 74" width="38" height="56" xmlns="http://www.w3.org/2000/svg">
          <rect x="1" y="6" width="42" height="64" rx="9" fill="#e8f4ff" stroke="#b0cce4" stroke-width="1.5"/>
          <rect x="12" y="1" width="20" height="9" rx="3" fill="#cce0f0"/>
          <text x="22" y="38" text-anchor="middle" fill="#003b6e" font-size="5" font-weight="700" font-family="sans-serif">NIVEA</text>
        </svg>
        <div class="v-size">600 ml</div>
        <div class="v-price">Ksh 3,500</div>
      </div>
    </div>

    <!-- Price -->
    <div class="price-row">
      <div class="price-main"><span class="price-currency">Ksh </span><span id="priceDisplay">300</span></div>
      <div class="price-old">Ksh 380</div>
      <div class="price-save">Save 21%</div>
    </div>

    <!-- Quantity -->
    <div class="section-label">Quantity</div>
    <div class="qty-row">
      <div class="qty-ctrl">
        <button class="qty-btn" onclick="changeQty(-1)">−</button>
        <div class="qty-val" id="qtyVal">1</div>
        <button class="qty-btn" onclick="changeQty(1)">+</button>
      </div>
      <span class="stock-info">✓ In stock — 48 units</span>
    </div>

    <!-- Actions -->
    <div class="actions">
      <button class="btn btn-check">📍 Check Availability</button>
      <button class="btn btn-cart">🛒 Add to Cart</button>
      <button class="btn btn-buy">⚡ Buy Now</button>
    </div>

    <!-- About -->
    <div class="about">
      <h3>About This Product</h3>
      <p>Deep moisturizing lotion with a smooth, non-greasy formula that helps keep skin soft, hydrated, fresh, and healthy for everyday skincare. Enriched with Deep Moisture Serum and natural almond oil for long-lasting 48-hour nourishment.</p>
      <div class="tags">
        <span class="tag">48h Moisture</span>
        <span class="tag">Non-Greasy</span>
        <span class="tag">Almond Oil</span>
        <span class="tag">Deep Serum</span>
        <span class="tag">Daily Use</span>
      </div>
    </div>

  </div>

  <!-- MORE IN PRODUCT -->
  <div class="more-section">
    <div class="more-header">
      <h2>More Products</h2>
      <a href="#">View all →</a>
    </div>
    <div class="more-grid">
      <!-- Card 1 -->
      <div class="more-card">
        <div class="mc-img">
          <svg viewBox="0 0 80 80" width="80" height="80" xmlns="http://www.w3.org/2000/svg">
            <circle cx="40" cy="40" r="32" fill="#f5e6c8"/>
            <text x="40" y="44" text-anchor="middle" fill="#8b6914" font-size="10" font-weight="