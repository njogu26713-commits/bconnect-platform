<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>PropVault — Landlord Dashboard</title>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet"/>
<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --bg: #0f0f13;
  --bg2: #16161d;
  --bg3: #1e1e28;
  --border: rgba(255,255,255,.07);
  --amber: #f5a623;
  --amber-dim: rgba(245,166,35,.12);
  --amber-glow: rgba(245,166,35,.25);
  --teal: #2ec4b6;
  --teal-dim: rgba(46,196,182,.1);
  --rose: #e05c7a;
  --rose-dim: rgba(224,92,122,.1);
  --green: #3ecf8e;
  --green-dim: rgba(62,207,142,.1);
  --text: #f0ede8;
  --text2: #9996a0;
  --text3: #6b6875;
  --card-radius: 16px;
  --transition: .2s cubic-bezier(.22,1,.36,1);
}

body {
  font-family: 'Outfit', sans-serif;
  background: var(--bg);
  color: var(--text);
  min-height: 100vh;
  display: flex;
  overflow-x: hidden;
}

/* ── SIDEBAR ── */
.sidebar {
  width: 240px;
  min-height: 100vh;
  background: var(--bg2);
  border-right: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  padding: 28px 0;
  position: fixed;
  left: 0; top: 0; bottom: 0;
  z-index: 50;
  animation: slideIn .5s cubic-bezier(.22,1,.36,1) both;
}
@keyframes slideIn { from { transform: translateX(-30px); opacity: 0; } }

.logo {
  padding: 0 24px 28px;
  border-bottom: 1px solid var(--border);
  margin-bottom: 24px;
}
.logo-mark {
  font-family: 'Playfair Display', serif;
  font-size: 1.4rem;
  color: var(--amber);
  letter-spacing: .5px;
}
.logo-sub { font-size: .72rem; color: var(--text3); letter-spacing: 1px; text-transform: uppercase; margin-top: 2px; }

.nav-section { padding: 0 12px; margin-bottom: 8px; }
.nav-label { font-size: .65rem; text-transform: uppercase; letter-spacing: 1.5px; color: var(--text3); padding: 6px 12px 8px; }
.nav-item {
  display: flex; align-items: center; gap: 10px;
  padding: 10px 12px; border-radius: 10px;
  font-size: .875rem; font-weight: 500; color: var(--text2);
  cursor: pointer; transition: all var(--transition);
  margin-bottom: 2px;
}
.nav-item:hover { background: var(--bg3); color: var(--text); }
.nav-item.active { background: var(--amber-dim); color: var(--amber); }
.nav-item.active .nav-icon { filter: none; }
.nav-icon { font-size: 1rem; width: 20px; text-align: center; }
.nav-badge {
  margin-left: auto; background: var(--rose);
  color: #fff; font-size: .65rem; font-weight: 700;
  padding: 2px 7px; border-radius: 20px;
}

.sidebar-footer {
  margin-top: auto;
  padding: 20px 16px 0;
  border-top: 1px solid var(--border);
}
.user-card {
  display: flex; align-items: center; gap: 10px;
  padding: 10px; border-radius: 10px;
  cursor: pointer; transition: background var(--transition);
}
.user-card:hover { background: var(--bg3); }
.avatar {
  width: 36px; height: 36px; border-radius: 50%;
  background: linear-gradient(135deg, var(--amber), #e07b00);
  display: grid; place-items: center;
  font-weight: 700; font-size: .875rem; color: #fff;
  flex-shrink: 0;
}
.user-name { font-size: .85rem; font-weight: 600; color: var(--text); }
.user-role { font-size: .72rem; color: var(--text3); }

/* ── MAIN ── */
.main {
  margin-left: 240px;
  flex: 1;
  padding: 32px 36px;
  animation: fadeUp .6s .1s cubic-bezier(.22,1,.36,1) both;
}
@keyframes fadeUp { from { opacity: 0; transform: translateY(16px); } }

/* ── TOPBAR ── */
.topbar {
  display: flex; align-items: center; justify-content: space-between;
  margin-bottom: 32px;
}
.page-title { font-family: 'Playfair Display', serif; font-size: 1.75rem; color: var(--text); }
.page-sub { font-size: .85rem; color: var(--text3); margin-top: 2px; }
.topbar-right { display: flex; align-items: center; gap: 12px; }
.icon-btn {
  width: 40px; height: 40px; border-radius: 10px;
  background: var(--bg2); border: 1px solid var(--border);
  display: grid; place-items: center;
  font-size: 1rem; cursor: pointer;
  position: relative; transition: border-color var(--transition);
}
.icon-btn:hover { border-color: var(--amber); }
.icon-btn .dot {
  position: absolute; top: 7px; right: 7px;
  width: 7px; height: 7px; border-radius: 50%;
  background: var(--amber); border: 2px solid var(--bg);
}
.add-btn {
  display: flex; align-items: center; gap: 7px;
  background: var(--amber); color: #111;
  border: none; border-radius: 10px;
  padding: 10px 18px; font-family: 'Outfit', sans-serif;
  font-size: .875rem; font-weight: 600; cursor: pointer;
  transition: all var(--transition);
  box-shadow: 0 4px 16px var(--amber-glow);
}
.add-btn:hover { transform: translateY(-1px); box-shadow: 0 6px 24px var(--amber-glow); }

/* ── KPI GRID ── */
.kpi-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 16px;
  margin-bottom: 28px;
}
.kpi {
  background: var(--bg2);
  border: 1px solid var(--border);
  border-radius: var(--card-radius);
  padding: 22px;
  position: relative; overflow: hidden;
  transition: border-color var(--transition), transform var(--transition);
  animation: fadeUp .6s both;
}
.kpi:nth-child(1) { animation-delay: .1s; }
.kpi:nth-child(2) { animation-delay: .15s; }
.kpi:nth-child(3) { animation-delay: .2s; }
.kpi:nth-child(4) { animation-delay: .25s; }
.kpi:hover { border-color: rgba(255,255,255,.14); transform: translateY(-2px); }
.kpi::before {
  content: '';
  position: absolute; top: 0; left: 0; right: 0; height: 2px;
  border-radius: var(--card-radius) var(--card-radius) 0 0;
}
.kpi.amber::before { background: var(--amber); }
.kpi.teal::before  { background: var(--teal); }
.kpi.green::before { background: var(--green); }
.kpi.rose::before  { background: var(--rose); }

.kpi-icon {
  width: 40px; height: 40px; border-radius: 10px;
  display: grid; place-items: center; font-size: 1.1rem;
  margin-bottom: 14px;
}
.kpi.amber .kpi-icon { background: var(--amber-dim); }
.kpi.teal  .kpi-icon { background: var(--teal-dim); }
.kpi.green .kpi-icon { background: var(--green-dim); }
.kpi.rose  .kpi-icon { background: var(--rose-dim); }

.kpi-label { font-size: .75rem; color: var(--text3); text-transform: uppercase; letter-spacing: .8px; margin-bottom: 6px; }
.kpi-value { font-family: 'Playfair Display', serif; font-size: 1.9rem; color: var(--text); line-height: 1; margin-bottom: 8px; }
.kpi-delta {
  font-size: .75rem; font-weight: 500; display: flex; align-items: center; gap: 4px;
}
.kpi-delta.up { color: var(--green); }
.kpi-delta.down { color: var(--rose); }

/* ── MIDDLE ROW ── */
.mid-row {
  display: grid;
  grid-template-columns: 1fr 360px;
  gap: 20px;
  margin-bottom: 24px;
}

/* ── REVENUE CHART ── */
.card {
  background: var(--bg2);
  border: 1px solid var(--border);
  border-radius: var(--card-radius);
  padding: 24px;
  animation: fadeUp .6s .3s both;
}
.card-header {
  display: flex; align-items: center; justify-content: space-between;
  margin-bottom: 22px;
}
.card-title { font-family: 'Playfair Display', serif; font-size: 1.05rem; color: var(--text); }
.card-sub { font-size: .75rem; color: var(--text3); margin-top: 2px; }
.chip {
  font-size: .72rem; font-weight: 600;
  padding: 4px 10px; border-radius: 20px;
  background: var(--green-dim); color: var(--green);
}

.chart-area { position: relative; height: 160px; }
.chart-bars {
  display: flex; align-items: flex-end; gap: 10px;
  height: 140px; padding-bottom: 0;
}
.bar-col { flex: 1; display: flex; flex-direction: column; align-items: center; gap: 6px; }
.bar-wrap { width: 100%; flex: 1; display: flex; align-items: flex-end; }
.bar {
  width: 100%; border-radius: 6px 6px 0 0;
  transition: opacity var(--transition);
  cursor: pointer;
  position: relative;
}
.bar:hover { opacity: .8; }
.bar.amber { background: linear-gradient(180deg, var(--amber) 0%, rgba(245,166,35,.4) 100%); }
.bar.dim   { background: var(--bg3); }
.bar-lbl { font-size: .65rem; color: var(--text3); }

.y-labels {
  position: absolute; left: -32px; top: 0; bottom: 20px;
  display: flex; flex-direction: column; justify-content: space-between;
}
.y-lbl { font-size: .62rem; color: var(--text3); }

/* ── OCCUPANCY ── */
.occ-card { animation-delay: .35s; }
.occ-ring { display: flex; justify-content: center; margin: 8px 0 20px; }
.ring-svg { transform: rotate(-90deg); }
.ring-bg { fill: none; stroke: var(--bg3); stroke-width: 10; }
.ring-fill { fill: none; stroke: var(--amber); stroke-width: 10; stroke-linecap: round;
  stroke-dasharray: 283; stroke-dashoffset: 42;
  transition: stroke-dashoffset 1s cubic-bezier(.22,1,.36,1);
}
.ring-center {
  position: absolute; top: 50%; left: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
}
.ring-pct { font-family: 'Playfair Display', serif; font-size: 1.6rem; color: var(--text); }
.ring-lbl2 { font-size: .7rem; color: var(--text3); }
.ring-wrap { position: relative; width: 110px; height: 110px; }

.occ-legend { display: flex; flex-direction: column; gap: 10px; }
.occ-row {
  display: flex; align-items: center; justify-content: space-between;
  font-size: .82rem;
}
.occ-dot { width: 8px; height: 8px; border-radius: 50%; margin-right: 8px; display: inline-block; }
.occ-name { color: var(--text2); display: flex; align-items: center; }
.occ-val { font-weight: 600; color: var(--text); }
.occ-bar-bg { flex: 1; height: 4px; background: var(--bg3); border-radius: 2px; margin: 0 12px; }
.occ-bar-fill { height: 100%; border-radius: 2px; }

/* ── BOTTOM ROW ── */
.bottom-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
}

/* ── TENANTS TABLE ── */
.tenants-card { animation-delay: .4s; }
.search-bar {
  display: flex; align-items: center; gap: 8px;
  background: var(--bg3); border: 1px solid var(--border);
  border-radius: 8px; padding: 8px 12px; margin-bottom: 16px;
}
.search-bar input {
  background: none; border: none; outline: none;
  font-family: 'Outfit', sans-serif; font-size: .85rem;
  color: var(--text); flex: 1;
}
.search-bar input::placeholder { color: var(--text3); }

.tenant-row {
  display: flex; align-items: center; gap: 12px;
  padding: 11px 0; border-bottom: 1px solid var(--border);
  transition: background var(--transition); border-radius: 8px; padding: 10px 8px;
  cursor: pointer;
}
.tenant-row:hover { background: var(--bg3); }
.tenant-row:last-child { border-bottom: none; }
.t-avatar {
  width: 34px; height: 34px; border-radius: 50%;
  display: grid; place-items: center;
  font-size: .78rem; font-weight: 700; color: #fff; flex-shrink: 0;
}
.t-name { font-size: .875rem; font-weight: 500; color: var(--text); }
.t-unit { font-size: .75rem; color: var(--text3); }
.t-status {
  margin-left: auto;
  font-size: .7rem; font-weight: 600;
  padding: 3px 9px; border-radius: 20px;
}
.t-status.paid { background: var(--green-dim); color: var(--green); }
.t-status.due { background: var(--amber-dim); color: var(--amber); }
.t-status.late { background: var(--rose-dim); color: var(--rose); }
.t-amount { font-size: .82rem; font-weight: 600; color: var(--text2); min-width: 64px; text-align: right; }

/* ── MAINTENANCE ── */
.maint-card { animation-delay: .45s; }
.maint-item {
  display: flex; gap: 12px;
  padding: 12px 8px; border-bottom: 1px solid var(--border);
  border-radius: 8px; transition: background var(--transition); cursor: pointer;
}
.maint-item:hover { background: var(--bg3); }
.maint-item:last-child { border-bottom: none; }
.maint-icon {
  width: 36px; height: 36px; border-radius: 9px;
  display: grid; place-items: center; font-size: .95rem; flex-shrink: 0;
}
.maint-title { font-size: .875rem; font-weight: 500; color: var(--text); }
.maint-unit { font-size: .75rem; color: var(--text3); margin-top: 2px; }
.maint-pri {
  margin-left: auto; font-size: .68rem; font-weight: 700;
  padding: 3px 9px; border-radius: 20px; align-self: flex-start;
  white-space: nowrap;
}
.maint-pri.high { background: var(--rose-dim); color: var(--rose); }
.maint-pri.med  { background: var(--amber-dim); color: var(--amber); }
.maint-pri.low  { background: var(--teal-dim); color: var(--teal); }
.maint-date { font-size: .7rem; color: var(--text3); margin-top: 4px; }

/* ── RESPONSIVE ── */
@media (max-width: 1100px) {
  .kpi-grid { grid-template-columns: repeat(2, 1fr); }
  .mid-row { grid-template-columns: 1fr; }
}
@media (max-width: 860px) {
  .sidebar { width: 60px; }
  .logo-mark, .logo-sub, .nav-item span, .nav-label, .user-name, .user-role { display: none; }
  .main { margin-left: 60px; padding: 20px; }
  .bottom-row { grid-template-columns: 1fr; }
}
</style>
</head>
<body>

<!-- SIDEBAR -->
<aside class="sidebar">
  <div class="logo">
    <div class="logo-mark">PropVault</div>
    <div class="logo-sub">Landlord Portal</div>
  </div>

  <div class="nav-section">
    <div class="nav-label">Main</div>
    <div class="nav-item active"><span class="nav-icon">🏠</span><span>Dashboard</span></div>
    <div class="nav-item"><span class="nav-icon">🏢</span><span>Properties</span></div>
    <div class="nav-item"><span class="nav-icon">👥</span><span>Tenants</span></div>
    <div class="nav-item"><span class="nav-icon">💳</span><span>Rent & Payments</span><span class="nav-badge">3</span></div>
  </div>

  <div class="nav-section">
    <div class="nav-label">Manage</div>
    <div class="nav-item"><span class="nav-icon">🔧</span><span>Maintenance</span><span class="nav-badge">5</span></div>
    <div class="nav-item"><span class="nav-icon">📄</span><span>Leases</span></div>
    <div class="nav-item"><span class="nav-icon">📊</span><span>Reports</span></div>
    <div class="nav-item"><span class="nav-icon">💬</span><span>Messages</span></div>
  </div>

  <div class="nav-section">
    <div class="nav-label">Settings</div>
    <div class="nav-item"><span class="nav-icon">⚙️</span><span>Settings</span></div>
  </div>

  <div class="sidebar-footer">
    <div class="user-card">
      <div class="avatar">JK</div>
      <div>
        <div class="user-name">James Kamau</div>
        <div class="user-role">Property Owner</div>
      </div>
    </div>
  </div>
</aside>

<!-- MAIN CONTENT -->
<main class="main">

  <!-- TOPBAR -->
  <div class="topbar">
    <div>
      <div class="page-title">Good morning, James 👋</div>
      <div class="page-sub">Monday, June 8, 2026 · 3 rent payments pending</div>
    </div>
    <div class="topbar-right">
      <div class="icon-btn">🔔<span class="dot"></span></div>
      <div class="icon-btn">📅</div>
      <button class="add-btn">+ Add Property</button>
    </div>
  </div>

  <!-- KPI CARDS -->
  <div class="kpi-grid">
    <div class="kpi amber">
      <div class="kpi-icon">💰</div>
      <div class="kpi-label">Monthly Revenue</div>
      <div class="kpi-value">Ksh 284,500</div>
      <div class="kpi-delta up">▲ 12.4% vs last month</div>
    </div>
    <div class="kpi teal">
      <div class="kpi-icon">🏢</div>
      <div class="kpi-label">Total Units</div>
      <div class="kpi-value">47</div>
      <div class="kpi-delta up">▲ 3 new this quarter</div>
    </div>
    <div class="kpi green">
      <div class="kpi-icon">✅</div>
      <div class="kpi-label">Occupancy Rate</div>
      <div class="kpi-value">85%</div>
      <div class="kpi-delta up">▲ 40 / 47 occupied</div>
    </div>
    <div class="kpi rose">
      <div class="kpi-icon">⚠️</div>
      <div class="kpi-label">Overdue Rent</div>
      <div class="kpi-value">Ksh 38,000</div>
      <div class="kpi-delta down">▼ 3 tenants behind</div>
    </div>
  </div>

  <!-- MIDDLE ROW -->
  <div class="mid-row">

    <!-- REVENUE CHART -->
    <div class="card">
      <div class="card-header">
        <div>
          <div class="card-title">Revenue Overview</div>
          <div class="card-sub">Rent collected per month — 2026</div>
        </div>
        <span class="chip">↑ 12.4%</span>
      </div>
      <div class="chart-area">
        <div class="chart-bars" id="chartBars">
          <!-- generated by JS -->
        </div>
      </div>
    </div>

    <!-- OCCUPANCY RING -->
    <div class="card occ-card">
      <div class="card-header">
        <div>
          <div class="card-title">Occupancy</div>
          <div class="card-sub">By property type</div>
        </div>
      </div>
      <div class="occ-ring">
        <div class="ring-wrap">
          <svg class="ring-svg" width="110" height="110" viewBox="0 0 110 110">
            <circle class="ring-bg" cx="55" cy="55" r="45"/>
            <circle class="ring-fill" cx="55" cy="55" r="45"/>
          </svg>
          <div class="ring-center">
            <div class="ring-pct">85%</div>
            <div class="ring-lbl2">Occupied</div>
          </div>
        </div>
      </div>
      <div class="occ-legend">
        <div class="occ-row">
          <div class="occ-name"><span class="occ-dot" style="background:var(--amber)"></span>Apartments</div>
          <div class="occ-bar-bg"><div class="occ-bar-fill" style="width:88%;background:var(--amber)"></div></div>
          <div class="occ-val">88%</div>
        </div>
        <div class="occ-row">
          <div class="occ-name"><span class="occ-dot" style="background:var(--teal)"></span>Bedsitters</div>
          <div class="occ-bar-bg"><div class="occ-bar-fill" style="width:79%;background:var(--teal)"></div></div>
          <div class="occ-val">79%</div>
        </div>
        <div class="occ-row">
          <div class="occ-name"><span class="occ-dot" style="background:var(--rose)"></span>Vacant</div>
          <div class="occ-bar-bg"><div class="occ-bar-fill" style="width:15%;background:var(--rose)"></div></div>
          <div class="occ-val">7 units</div>
        </div>
      </div>
    </div>
  </div>

  <!-- BOTTOM ROW -->
  <div class="bottom-row">

    <!-- TENANTS -->
    <div class="card tenants-card">
      <div class="card-header">
        <div>
          <div class="card-title">Tenants — Rent Status</div>
          <div class="card-sub">June 2026</div>
        </div>
        <a href="#" style="font-size:.78rem;color:var(--amber);text-decoration:none;">View all</a>
      </div>
      <div class="search-bar">
        <span>🔍</span>
        <input type="text" placeholder="Search tenant or unit…"/>
      </div>
      <div id="tenantList"></div>
    </div>

    <!-- MAINTENANCE -->
    <div class="card maint-card">
      <div class="card-header">
        <div>
          <div class="card-title">Maintenance Requests</div>
          <div class="card-sub">5 open tickets</div>
        </div>
        <a href="#" style="font-size:.78rem;color:var(--amber);text-decoration:none;">View all</a>
      </div>
      <div id="maintList"></div>
    </div>

  </div>
</main>

<script>
// ── CHART ──
const months = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
const values = [190, 210, 195, 230, 248, 284, 0, 0, 0, 0, 0, 0];
const maxVal = 300;
const container = document.getElementById('chartBars');
months.forEach((m, i) => {
  const h = values[i] ? Math.round((values[i] / maxVal) * 120) : 0;
  const col = document.createElement('div');
  col.className = 'bar-col';
  col.innerHTML = `
    <div class="bar-wrap">
      <div class="bar ${values[i] ? 'amber' : 'dim'}" style="height:${h || 18}px"
        title="${values[i] ? 'Ksh ' + values[i].toLocaleString() + 'k' : 'No data'}">
      </div>
    </div>
    <span class="bar-lbl">${m}</span>
  `;
  container.appendChild(col);
});

// ── TENANTS ──
const tenants = [
  { name: 'Mercy Wanjiku', unit: 'A-104 · 1BR', amount: 'Ksh 18,000', status: 'paid', color: '#2ec4b6', init: 'MW' },
  { name: 'Daniel Oduya', unit: 'B-202 · Bedsit', amount: 'Ksh 9,500', status: 'due', color: '#f5a623', init: 'DO' },
  { name: 'Grace Njeri', unit: 'A-201 · 2BR', amount: 'Ksh 26,000', status: 'paid', color: '#3ecf8e', init: 'GN' },
  { name: 'Kevin Mwangi', unit: 'C-301 · 1BR', amount: 'Ksh 16,000', status: 'late', color: '#e05c7a', init: 'KM' },
  { name: 'Amina Hassan', unit: 'B-105 · Bedsit', amount: 'Ksh 8,500', status: 'paid', color: '#a78b